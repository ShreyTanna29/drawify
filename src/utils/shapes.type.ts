export type Rectangle = {
  id: string;
  x: number;
  y: number;
  height: number;
  width: number;
};

export type Arrow = {
  id: string;
  points: number[];
};

export type Circle = {
  id: string;
  x: number;
  y: number;
  radius: number;
};
